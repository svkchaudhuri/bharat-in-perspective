{
  "name": "bharat-in-perspective",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "India-centred, source-transparent development comparisons and live hazard context",
  "scripts": {
    "start": "node server.mjs",
    "refresh": "node scripts/refresh.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
